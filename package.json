{
  "name": "mivimoose-draw",
  "version": "2.0.0",
  "description": "Mivimoose Draw — modern multiplayer drawing & guessing game with accounts, public matches and custom word lists",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon server.js",
    "smoke": "node test/smoke.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "socket.io-client": "^4.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
